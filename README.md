# Simple chess engine


## Run project as a developer

1. Install dependencies:
    ```bash
    npm install
    ```

2. Compile typescript in watch mode:
    ```bash
    npm run dev
    ```

2. Next run parcel:

    ```bash
    npm start
    ```